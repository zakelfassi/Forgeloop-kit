# PR Triage 2026-03-05

## Keep / Salvage

- `#25` `fix(loop): harden ralph-state security, atomicity, and validation`
  Keep as a focused salvage candidate after the CI/path bootstrap fix.
- `#26` `feat(llm): explicit build task routing + provider hardening`
  Keep as a focused salvage candidate after the CI/path bootstrap fix.
- `#27` `feat(template): browser-agent.dev e2e verify gate`
  Hold until after control-plane stabilization. Useful, but it should land after the repo reliably fails closed.

## Supersede / Rewrite

- `#1` through `#24`
  Treat as a stacked historical draft lineage, not as independent merge candidates.
  The worthwhile ideas should be re-cut into a small number of clean successor PRs instead of reviewed one-by-one.

## Why

- The open `loop-eng/*` drafts are cumulative branches, so reviewing them independently creates noise.
- GitHub Actions was globally broken by the shared repo-root path bootstrap bug, so prior red CI on those PRs was not useful merge signal.
- The cleanest path is:
  1. land control-plane stabilization,
  2. re-cut the few changes that still matter,
  3. close the stale draft stack as superseded.
